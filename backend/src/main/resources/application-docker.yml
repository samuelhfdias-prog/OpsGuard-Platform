# Profile ativado quando SPRING_PROFILES_ACTIVE=docker
# Sobrescreve apenas a URL do datasource para usar o hostname do Docker network
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/rodojacto_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: rodojacto
    password: rodojacto123
